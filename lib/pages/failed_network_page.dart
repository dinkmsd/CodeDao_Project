import 'package:flutter/material.dart';

class FailedNetworkPage extends StatelessWidget {
  const FailedNetworkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Failed network'),
    );
  }
}
